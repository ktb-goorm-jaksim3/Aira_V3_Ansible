---
- name: Create Kubernetes namespaces
  hosts: localhost
  tasks:
    - name: Ensure namespaces exist
      kubernetes.core.k8s:
        kind: Namespace
        name: "{{ item }}"
        state: present
      loop:
        # - infrastructure // argocd로 대신
        - monitoring
        - database