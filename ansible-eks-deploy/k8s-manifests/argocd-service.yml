apiVersion: v1
kind: Service
metadata:
  name: argocd-server
  namespace: argocd
spec:
  selector:
    app: argocd-server
  type: NodePort  # ✅ NodePort 방식으로 직접 접근 가능하게 설정
  ports:
    - port: 80
      targetPort: 8080
      nodePort: 30080  # ✅ 직접 노드에서 접근 가능하도록 포트 지정